import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tradeinsights.app',
  appName: 'Trade Insights',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
